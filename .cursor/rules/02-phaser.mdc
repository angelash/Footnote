---
description: Phaser 3 游戏引擎开发规范
globs: ["**/scenes/**/*.ts", "**/entities/**/*.ts", "**/systems/**/*.ts"]
alwaysApply: false
---

# Phaser 3 开发规范

## 场景生命周期

```
init(data) → preload() → create() → update(time, delta) → shutdown()
```

- `init`: 接收场景间传递的数据
- `preload`: 仅加载本场景特有资源（全局资源在 PreloadScene）
- `create`: 创建游戏对象、设置事件
- `update`: 游戏循环（60fps）
- `shutdown`: 清理资源和事件监听

## 标准场景

| Key | 职责 |
|-----|------|
| BootScene | 启动检查、浏览器兼容性 |
| PreloadScene | 全局资源预加载 |
| MenuScene | 主菜单 |
| GameScene | 游戏主场景 |

## 资源命名规范

```
{type}_{identifier}

bg_c0z1           # 背景
char_cenhui       # 角色
ui_button_primary # UI元素
sfx_click         # 音效
bgm_main          # 背景音乐
```

## 输入系统规范

### ⚠️ 键名大小写约定（重要！）

| 方法 | 返回键名格式 |
|------|-------------|
| `createCursorKeys()` | **小写**: `left`, `right`, `up`, `down` |
| `addKeys('W,A,S,D')` | **大写**: `W`, `A`, `S`, `D` |

**核心规则**：访问 `addKeys` 返回的键时必须使用**大写**！

```typescript
// ✅ 正确
const up = this._moveKeys.W?.isDown;

// ❌ 错误 - 返回 undefined
const up = this._moveKeys.w?.isDown;
```

### 常用按键代码

- 字母：`A` - `Z`（大写）
- 数字：`ZERO`, `ONE`, `TWO` ... `NINE`
- 功能：`SPACE`, `ENTER`, `ESC`, `TAB`, `SHIFT`
- 方向：`UP`, `DOWN`, `LEFT`, `RIGHT`

## 场景视角与移动

- **视角**：第三人称俯视角（Top-down 2D）
- **深度排序**：默认 `depth = y`（y-sort）
- **移动控制**：方向键 + WASD
- **对角线归一化**：避免斜向移动更快
- **物理引擎**：Arcade Physics

## 事件系统

### 全局事件总线
```typescript
// 发送
this.game.events.emit('zone:enter', { zoneId: 'C0-Z1' });

// 监听
this.game.events.on('zone:enter', this._handler, this);

// 清理（必须！）
this.game.events.off('zone:enter', this._handler, this);
```

### 场景间通信
```typescript
// 切换时传数据
this.scene.start('GameScene', { zoneId: 'C1-Z1' });

// 访问其他场景
this.scene.get('UIOverlay').showCard(card);
```

## 交互对象

```typescript
sprite.setInteractive({ useHandCursor: true })
  .on('pointerdown', handler)
  .on('pointerover', () => sprite.setTint(0xcccccc))
  .on('pointerout', () => sprite.clearTint());
```

## 性能原则

1. **对象池**：频繁创建/销毁的对象使用 `Group`
2. **离屏禁用**：`setActive(false)` 停止 update
3. **Camera裁剪**：`setBounds` 限制渲染范围
4. **静态合并**：多个静态图片用 `RenderTexture` 合并

## 调试

```typescript
if (import.meta.env.DEV) {
  this.physics.world.createDebugGraphic();
  this.input.enableDebug(this._player);
}
```

## 参考实现

- 玩家移动：`src/scenes/GameScene.ts`
- 场景组装：`src/systems/scene/SceneAssembler.ts`
- 输入处理：`src/systems/input/TouchControls.ts`
