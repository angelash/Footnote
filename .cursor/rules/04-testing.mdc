---
description: 测试规范和自动化测试指南
globs: ["**/tests/**/*.ts", "**/*.test.ts", "**/*.spec.ts"]
alwaysApply: false
---

# 测试规范

## 核心原则

### 测试质量检查清单

每个测试用例必须回答：
- [ ] **测试了什么功能？**（描述清晰）
- [ ] **预期结果是什么？**（有断言）
- [ ] **如何验证结果？**（断言具体）
- [ ] **测试在正确的上下文执行吗？**（场景/状态）
- [ ] **测试能检测到 Bug 吗？**（不是假阳性）

### 测试必须验证效果

❌ **错误**：只验证"不崩溃"
```typescript
// 只按键，不验证效果
await page.keyboard.press('KeyW');
await page.waitForTimeout(200);
// 没有断言！
```

✅ **正确**：验证状态变化
```typescript
const initialPos = getPlayerPosition();
await page.keyboard.press('KeyW');
const newPos = getPlayerPosition();
expect(newPos.y).toBeLessThan(initialPos.y); // 断言位置变化
```

### 测试必须在正确上下文执行

- 移动测试 → 必须在 **GameScene** 执行，不是 MenuScene
- 对话测试 → 必须先**触发对话**，不是空状态
- 能力测试 → 必须先**解锁能力**，不是初始状态

## 测试分层

### 单元测试（Vitest）
- **目标**：核心逻辑、纯函数、状态计算
- **位置**：`tests/unit/` 或同目录 `*.test.ts`
- **Mock**：隔离 Phaser、IndexedDB 等外部依赖

### E2E测试（Playwright）
- **目标**：完整用户流程、多系统协作
- **位置**：`tests/e2e/`
- **原则**：模拟真实用户操作，验证可见结果

## 覆盖率要求

| 层级 | 目标覆盖率 | 系统 |
|------|-----------|------|
| **核心系统** | ≥80% | WorldState, NarrativeEngine, DialogueManager, CardSystem, SaveManager |
| **游戏逻辑** | ≥70% | ZoneManager, AbilitySystem, InputSystem |
| **UI组件** | ≥50% | DialogueBox, CardViewer, InventoryUI |

### E2E 必须覆盖

- [ ] 全部主线流程（C0-C5 + 终章）
- [ ] 三结局路径（A/B/C）
- [ ] 关键分支选择
- [ ] 伏笔触发和回收

## 测试命名规范

```typescript
describe('系统/模块名', () => {
  describe('功能分组', () => {
    it('应该 + 具体行为', () => {});
    it('当 X 时应该 Y', () => {});
    it('不应该 + 错误行为', () => {});
  });
});
```

## 测试ID属性规范

Phaser 对象使用 `setData` 添加测试标识：
```typescript
button.setData('testid', 'start-button');
```

E2E 通过 `evaluate` 访问游戏状态：
```typescript
const state = await page.evaluate(() => window.__DEBUG_STATE__);
```

## 回归测试原则

发现 Bug 后：
1. **先写失败的测试**复现 Bug
2. 修复代码使测试通过
3. 测试文件添加注释说明 Bug 来源
4. 确保 CI 包含该测试防止回归

## 参考文件

- 单元测试示例：`tests/unit/scenes/GameScene.test.ts`
- E2E 测试示例：`tests/e2e/game.spec.ts`
- 测试配置：`vitest.config.ts`, `playwright.config.ts`
