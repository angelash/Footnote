---
description: 美术资源规范和AI生图操作指南
globs: ["**/assets/**/*", "**/*.svg", "**/*.png", "**/*.webp"]
alwaysApply: false
---

# 美术资源规范

## 视觉风格（项目级）

### 整体风格
- **主风格**: 像素风 + 赛博朋克美学
- **氛围**: 科幻悬疑、冷峻克制、深空感
- **调色**: 深色主调 + 荧光强调色（青色/红色/金色）

### 色彩系统
```css
/* 主色调 */
--bg-primary: #0A0A0F;       /* 深空黑 */
--bg-secondary: #141419;      /* 次级背景 */

/* 强调色 */
--accent-depth: #00FFAA;      /* 深度视觉 - 荧光绿 */
--accent-time: #FF4444;       /* 时间污染 - 警告红 */
--accent-system: #4A9EFF;     /* 系统UI - 科技蓝 */
--accent-field: #FFD700;      /* 字段接受 - 金色 */
```

### 分辨率标准
```
基准分辨率: 750 × 1334px (竖屏手机)
设计尺寸: @2x
```

---

## 资源类型规格

| 类型 | 格式 | 尺寸 | 命名规则 |
|------|------|------|----------|
| 角色头像 | WebP | 512×512px | {name}_{expression}_{n}.webp |
| 场景背景 | WebP/SVG | 750×1334px | bg_{chapter}_{scene}_{n}.webp |
| 场景物件 | WebP | 各异（透明背景） | {category}_{name}_{n}.webp |
| 特效 | WebP | 各异（透明背景） | {effect_name}_{n}.webp |
| 动画帧 | WebP | 横排序列帧 | {name}_strip.webp |

---

## 目录结构
```
assets/
├── images/
│   ├── characters/portraits/{name}/   # 角色头像
│   ├── backgrounds/{chapter}/         # 场景背景
│   ├── objects/{category}/            # 场景物件
│   ├── effects/{type}/                # 特效
│   └── ui/                            # UI元素
└── audio/                             # 音频资源
```

---

## AI生图操作流程

### 1. 访问生图平台
- URL: https://artflow.gz4399.com/ai
- 进入"对话生图"功能

### 2. 提示词约束（必须包含）
```
【核心约束】
- pixel art style（像素风格）
- cyberpunk aesthetic（赛博朋克美学）
- dark background / transparent background（根据资产类型）
- portrait view / vertical composition（竖屏构图）
- consistent lighting（统一光源）
- no text, no watermark（无文字无水印）
```

### 3. 质量检查清单
生成后必须检查：
- [ ] 风格一致（像素风+赛博朋克）
- [ ] 朝向/构图正确
- [ ] 透明背景正确（物件/特效/头像）
- [ ] 无文字/水印
- [ ] 表情与命名匹配

### 4. 资产分类
```
通过检查 → assets/images/{对应目录}/
未通过   → assets/images/{类型}/rejected/
待处理   → assets/images/{类型}/remaining/
```

---

## 开发期占位策略

1. **能用则用**：现有资源满足需求优先使用
2. **不满足则占位**：生成占位图
3. **记录需求**：在 `docs/资产需求跟踪.md` 记录

### 占位图规范
```
命名: placeholder_{类型}_{名称}.png
内容: 纯色背景 + 类型文字标识
```

---

## 详细规范文档

具体资产设计细节请参考：
- `docs/art/角色设计规范.md` - 8个角色的详细设计
- `docs/art/场景设计规范.md` - 6大区域+各Zone设计
- `docs/art/特效设计规范.md` - 能力特效设计
- `docs/art/UI设计规范.md` - UI组件设计
- `docs/art/提示词模板.md` - AI生图提示词模板
