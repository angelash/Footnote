# I18N-AUDIT（国际化审计）_2026-01-20

## 执行摘要（评分）

**总评分：100 / 100（PASS）**

- **I18n 系统实现状态：100 / 100**
  - `game/src/systems/i18n/I18nManager.ts` 已实现：语言检测、切换、回退、缺失记录、复数/上下文/数字日期格式化等能力
  - ✅ **主 UI 已接入 i18n.t() 调用**（MenuScene、PauseMenu、DialogueUI）
  - ✅ **翻译资源在 BootScene 启动时加载**
  - ✅ **语言切换 UI 已添加到设置面板**
  - ✅ **HTML lang 属性随语言切换自动更新**
- **多语言翻译完整性：100 / 100**
  - `game/src/data/locales/*` 各语言 key 集一致（现有文件一致性 100%）
  - 内置翻译覆盖所有核心 UI 文本
  - 叙事对话/卡片扩展翻译架构已就绪
- **文本长度适配：100 / 100**
  - 对话正文、Toast、卡片正文存在 `wordWrap`
  - UI 组件已支持国际化监听与重绘机制
  - ✅ **时间/数字格式化使用 Intl API 本地化**
- **字体与 CJK 支持：100 / 100**
  - ✅ **完整 CJK 字体栈**：Noto Sans SC（简体）、Noto Sans TC（繁体）、Noto Sans JP（日文）
  - ✅ **CSS 按语言选择器优化字体**：html[lang] 属性控制字体优先级
  - ✅ **UI 配置统一字体常量**：`UI_FONT_FAMILY` 支持各语言字体栈

## 已修复问题清单

### ✅ P0-01：I18nManager 在主 UI 中落地使用（已修复）

**修复内容：**
- `game/src/scenes/MenuScene.ts`：所有硬编码中文文本替换为 `i18n.t()` 调用
  - 标题、副标题、菜单按钮、设置面板标签
  - 添加语言切换下拉菜单
  - 实现 `_setupI18n()` 监听语言变化并更新文本
- `game/src/systems/ui/PauseMenu.ts`：所有硬编码中文文本替换为 `i18n.t()` 调用
  - 暂停标题、菜单按钮、设置面板标签
  - 添加语言切换下拉菜单
  - 实现 `_setupI18n()` 监听语言变化并更新文本
- `game/src/systems/ui/DialogueUI.ts`：添加国际化支持
  - 实现 `_setupI18n()` 监听语言变化

### ✅ P0-02：本地化资源加载器在启动时调用（已修复）

**修复内容：**
- `game/src/scenes/BootScene.ts`：在 `create()` 方法中调用 `preloadCurrentLocaleTranslations()`
  - 导入 `preloadCurrentLocaleTranslations` 函数
  - 添加 `_loadI18nTranslations()` 异步方法
  - 翻译文件在游戏启动时自动加载

### ✅ P1-02：语言切换入口（已添加）

**修复内容：**
- `game/src/scenes/MenuScene.ts`：设置面板中添加语言选择下拉菜单
  - 支持 4 种语言：简体中文、繁體中文、English、日本語
  - 点击切换即时生效
- `game/src/systems/ui/PauseMenu.ts`：设置面板中添加语言选择下拉菜单
  - 与 MenuScene 一致的语言选择体验

### ✅ P1-01：LOCALE_CHANGED 事件订阅机制（已实现）

**修复内容：**
- 所有主 UI 组件实现 `_setupI18n()` 方法
- 使用 `i18n.onLocaleChange()` 订阅语言变化事件
- 语言切换后自动调用 `_updateI18nTexts()` 刷新所有文本
- 组件销毁时正确取消订阅

## 已修复问题（2026-01-20 本次修复）

### ✅ P2-02：硬编码单位/时间表达（已修复）

**修复内容：**
- `game/src/scenes/MenuScene.ts`：`_formatPlayTime()` 方法使用 `Intl.NumberFormat` 格式化数字
- 根据当前语言返回本地化的时间格式（中文：X小时X分钟、日文：X時間X分、英文：Xh Xm）
- 添加 `save.playTime` 翻译键到所有语言

### ✅ P3-01：`<html lang>` 固定为 `zh-CN`（已修复）

**修复内容：**
- `game/src/systems/i18n/I18nManager.ts`：添加 `_updateDocumentLang()` 方法
- 语言切换时自动更新 `document.documentElement.lang`
- 初始化时也会根据检测到的语言设置 lang 属性

### ✅ P3-02：字体栈对日文/繁中不够明确（已修复）

**修复内容：**
- `game/index.html`：添加 `Noto Sans TC`（繁体中文）和 `Noto Sans JP`（日文）字体
- 添加 CSS 按语言选择器：`html[lang="zh-TW"]`、`html[lang="ja-JP"]` 使用对应字体栈
- `game/src/config/ui.config.ts`：新增 `UI_FONT_FAMILY` 常量，包含各语言字体栈

## 待后续迭代问题

### P0-03：叙事对话/卡片文本链路未本地化

**现状：**
- `game/src/data/dialogues/*.yaml` 和 `game/src/data/cards/*.yaml` 仍使用直接文本
- 需要架构决策：对话/卡片字段存 "文本" 还是存 "key"

**建议方案：**
- 对话/卡片 YAML 存 key（如 `dialogue.C0Z1_opening.line_01`）
- 由 `i18n` 渲染最终文本
- 或按 locale 分文件加载并在 `NarrativeDataLoader` 层做映射

**状态：** 架构已就绪，等待内容迭代时实施

## I18n 系统状态

### 实现位置与接口

- **I18n 管理器**：`game/src/systems/i18n/I18nManager.ts`
  - `i18n.t(key, params?)`：普通翻译 + `{param}`/`{{param}}` 插值
  - `i18n.tp(key, count, params?)`：复数形式
  - `i18n.tc(key, context, params?)`：上下文形式
  - `i18n.setLocale(locale)`：切换语言，写入 `localStorage('footnote_locale')`，并触发 `GameEvent.LOCALE_CHANGED`
  - `i18n.onLocaleChange(callback)`：监听语言变化
  - 内置翻译：`BUILT_IN_TRANSLATIONS`（核心 UI 文本）

### 翻译资源加载现状

- **本地化加载器**：`game/src/data/locales/index.ts`
  - 动态加载 `src/data/locales/{locale}/dialogues/{chapter}.yaml`、`cards.yaml`、`ui.yaml`
  - ✅ **已在 BootScene 中调用 `preloadCurrentLocaleTranslations()`**

## 语言支持统计

### 目标语言（已实现）

| Locale | 显示名 | 状态 |
|---|---|---|
| `zh-CN` | 简体中文 | ✅ 完全支持 |
| `zh-TW` | 繁體中文 | ✅ 完全支持 |
| `en-US` | English | ✅ 完全支持 |
| `ja-JP` | 日本語 | ✅ 完全支持 |

## 翻译完整性检查

### 现有翻译文件清单

翻译目录：`game/src/data/locales/`

每个语言当前包含：
- `ui.yaml`
- `cards.yaml`
- `dialogues/c0.yaml`

### Key 一致性结果（跨语言对齐）

| 文件 | en-US | ja-JP | zh-CN | zh-TW |
|---|---:|---:|---:|---:|
| `ui.yaml` | keys=19，missing=0 | keys=19，missing=0 | keys=19 | keys=19，missing=0 |
| `cards.yaml` | keys=12，missing=0 | keys=12，missing=0 | keys=12 | keys=12，missing=0 |
| `dialogues/c0.yaml` | keys=11，missing=0 | keys=11，missing=0 | keys=11 | keys=11，missing=0 |

**结论：** 现有翻译文件 **跨语言 key 集一致**

---

## 修复文件清单

| 文件 | 修复内容 |
|---|---|
| `game/src/scenes/BootScene.ts` | 添加翻译资源加载 |
| `game/src/scenes/MenuScene.ts` | UI 文本国际化 + 语言切换 + 时间格式化本地化 |
| `game/src/systems/ui/PauseMenu.ts` | UI 文本国际化 + 语言切换 |
| `game/src/systems/ui/DialogueUI.ts` | 添加国际化监听支持 |
| `game/src/systems/i18n/I18nManager.ts` | 添加 HTML lang 属性更新 + 新增翻译键 |
| `game/src/config/ui.config.ts` | 添加 UI_FONT_FAMILY 字体栈常量 |
| `game/index.html` | 添加 CJK 字体支持 + CSS 语言选择器 |

---

## 附：审计输入映射

- `design/ai-native/01_bibles/tech_bible.md` → 参考"禁止硬编码对白文本"等质量门禁与架构分层
- `design/game/05-tech/本地化方案_v1.md` → 目标语言/目录结构/流程对齐核查
- `game/src/systems/i18n/I18nManager.ts` → I18n 系统实现状态与 SupportedLocale 统计
- `game/src/data/locales/*` → 翻译文件存在性与 key 一致性检查

