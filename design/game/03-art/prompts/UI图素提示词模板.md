# UI图素像素艺术提示词模板

> 用于AI生图批量生成UI元素资产

---

## 通用前缀
```
pixel art game UI element, transparent background PNG with alpha, clean pixel edges, limited dark color palette with cyan accents, retro game style, no anti-aliasing, centered composition, crisp UI design
```

---

## 1. 按钮 (Buttons)

### 1.1 主按钮 (Primary Button)
```
pixel art game UI button, primary action button, rectangular shape with rounded corners, dark gray base with cyan highlight border, gradient shading, button state default, centered text area, transparent background PNG with alpha, clean pixel edges, limited dark color palette with cyan accents, retro game style, no anti-aliasing, 128x48 pixels
```

### 1.2 次按钮 (Secondary Button)
```
pixel art game UI button, secondary action button, rectangular shape with subtle border, dark outline on transparent background, minimalist design, button state default, centered text area, transparent background PNG with alpha, clean pixel edges, limited dark color palette, retro game style, no anti-aliasing, 128x48 pixels
```

### 1.3 图标按钮 - 菜单 (Menu Icon Button)
```
pixel art game UI icon button, menu hamburger icon, three horizontal lines, circular or square button frame, dark background with cyan accent, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 48x48 pixels
```

### 1.4 图标按钮 - 背包 (Inventory Icon Button)
```
pixel art game UI icon button, inventory bag icon, backpack or pouch shape, circular or square button frame, dark background with warm accent, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 48x48 pixels
```

### 1.5 图标按钮 - 设置 (Settings Icon Button)
```
pixel art game UI icon button, settings gear icon, cogwheel shape, circular or square button frame, dark background with neutral accent, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 48x48 pixels
```

---

## 2. 面板 (Panels)

### 2.1 对话框面板 (Dialogue Panel)
```
pixel art game UI panel, dialogue box frame, wide rectangular shape for text display, dark semi-transparent background, subtle pixel border with corner decorations, bottom of screen style, space for character portrait on left, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 640x160 pixels
```

### 2.2 菜单面板 (Menu Panel)
```
pixel art game UI panel, main menu background panel, dark gradient background, ornate pixel border frame, center screen placement, elegant yet minimalist design, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 400x500 pixels
```

### 2.3 卡片面板 (Card Panel)
```
pixel art game UI panel, item card display frame, vertical rectangular card shape, dark background with metallic border, space for icon and text, collectable card style, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 200x280 pixels
```

### 2.4 系统提示框 (System Message Box)
```
pixel art game UI panel, system notification popup, small rectangular alert box, warning style with yellow/red accent border, dark background, icon space on left side, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 320x80 pixels
```

---

## 3. 指示器 (Indicators)

### 3.1 进度条 (Progress Bar)
```
pixel art game UI element, horizontal progress bar, loading bar design, dark background track with cyan fill indicator, rounded ends, percentage display style, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 200x24 pixels
```

### 3.2 计数器UI - R值 (Counter R)
```
pixel art game UI element, stat counter display, letter R indicator, dark circular or hexagonal frame, digital number display style, red accent color theme, mysterious residue counter, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 80x80 pixels
```

### 3.3 计数器UI - P值 (Counter P)
```
pixel art game UI element, stat counter display, letter P indicator, dark circular or hexagonal frame, digital number display style, purple accent color theme, observer pressure counter, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 80x80 pixels
```

### 3.4 计数器UI - W值 (Counter W)
```
pixel art game UI element, stat counter display, letter W indicator, dark circular or hexagonal frame, digital number display style, cyan accent color theme, world readability counter, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 80x80 pixels
```

### 3.5 互动提示 (Interaction Prompt)
```
pixel art game UI element, interaction prompt icon, exclamation mark or hand pointer, glowing floating indicator, attention grabber design, pulsing animation frame, transparent background PNG with alpha, clean pixel edges, retro game style, no anti-aliasing, 32x48 pixels
```

---

## 文件命名规范

| 类型 | 前缀 | 示例 |
|------|------|------|
| 主按钮 | btn_primary_ | btn_primary_default.webp |
| 次按钮 | btn_secondary_ | btn_secondary_default.webp |
| 图标按钮 | btn_icon_ | btn_icon_menu.webp |
| 面板 | panel_ | panel_dialogue.webp |
| 进度条 | indicator_progress_ | indicator_progress_bar.webp |
| 计数器 | indicator_counter_ | indicator_counter_r.webp |

---

*创建于: 2024-12-24*

