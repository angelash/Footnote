{
  "name": "Fixed Flow Pipeline (WSL Proxy)",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "fixed-flow",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook-trigger",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "fixed-flow-webhook"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://localhost:3210/fixed-flow",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.body || {} }}",
        "options": {
          "timeout": 600000
        }
      },
      "id": "call-runner",
      "name": "Call WSL Runner",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [540, 300],
      "notes": "WSL Runner handles: stage logs, serial lock, cursor-agent execute, validate, git commit/push, notify. All exec commands run in WSL environment via the runner service."
    }
  ],
  "connections": {
    "Webhook Trigger": {
      "main": [[{ "node": "Call WSL Runner", "type": "main", "index": 0 }]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [
    { "name": "AI-Native" },
    { "name": "Fixed-Flow" },
    { "name": "WSL-Proxy" }
  ],
  "versionId": "fixedflow-proxy-v1"
}
